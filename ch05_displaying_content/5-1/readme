一、text/template

模板就是嵌入了动作的文本。
Go语言拥有通用模板引擎库text/template，它可以处理任意格式的文本。

-----------


二、html/template

除此之外，Go语言还拥有专门为HTML格式而设的模板引擎库html/template。
a.模板中的动作默认使用两个大括号{{   }}包围，
b.如果用户有需要，也可以通过模板引擎提供的方法自行指定其他定界符。
c.与php等语言一样，html在服务器端渲染生成最终网页返回给浏览器，适合快速做简易的后台

可以尝试实现如下:
取值   循环
选择   函数调用
